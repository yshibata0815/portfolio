<div class="users">
	<% provide(:title, 'All users') %>
		<h1>All users</h1>

		<ul class="users">
  		<% @users.each do |user| %>
    		<li class="user-name">
      			<%= link_to user.name, user %>
    		</li>
  		<% end %>
		</ul>
</div>